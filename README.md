# BBQ Maneuver — GitHub Pages Compact Version

This package uses the same working architecture as
`BPPV_GitHubPages_fixed_frames_compact.zip`.

Upload:
- `index.html` → repository root
- `data/simulation.json` → existing `data` folder
- `js/simulator.js` → existing `js` folder

The BBQ simulation contains two otoconia traces. All named animation
frames from the original HTML are preserved, while static geometry is
stored once to keep the data compact.
